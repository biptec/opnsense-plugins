name: os-bind quality

on:
  pull_request:
    paths:
      - "dns/bind/**"
      - ".github/workflows/bind-quality.yml"
  push:
    branches:
      - master
    paths:
      - "dns/bind/**"
      - ".github/workflows/bind-quality.yml"

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install PHP CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y php-cli

      - name: Validate PHP
        run: |
          find dns/bind/src/opnsense/mvc/app -name '*.php' -print0 \
            | xargs -0 -n1 php -l

      - name: Validate XML
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import xml.etree.ElementTree as ET

          for path in sorted(Path("dns/bind/src/opnsense/mvc/app").rglob("*.xml")):
              ET.parse(path)
              print(f"XML OK: {path}")
          PY

      - name: Validate Python and shell
        run: |
          find dns/bind/src/opnsense -name '*.py' -print0 \
            | xargs -0 -n1 python3 -m py_compile
          find dns/bind/src/opnsense -name '*.sh' -print0 \
            | xargs -0 -n1 sh -n

      - name: Run cleanup unit tests
        run: python3 -m unittest dns/bind/tests/test_service.py

      - name: Check whitespace
        run: git diff --check
